[project]
name = "warmstart-eval"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "transformers"
]

[tool.uv.sources]
lighteval = { git = "https://github.com/belatijagad/lighteval.git" }
